<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>File Uploads</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #9999db;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }


    nav {
      width: 100%;
      background-color: #d3bc39;
      padding: 1.5rem 0;
      text-align: center;
      font-size: 2.5rem;
      font-weight: bold;
      color: #130f0f;
      margin-top: 1%;
    
      border: 4px solid #130f0f;
      box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
      position: fixed; /* Fixed position to keep it at the top */
      top: 0; /* Stick to the top */
      z-index: 1000;  }

    /* Container for the form */
    .container {
      background-color: #d3bc39;
      padding: 2rem;
      border-radius: 8px;
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
      text-align: center;
      max-width: 500px;
      width: 100%;
      margin-top: 80px; /* Added spacing from navbar */
    }

    h1 {
      color: #333;
      margin-bottom: 1.5rem;
      font-size: 2rem;
    }

    form {
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    input[type="file"] {
      margin-bottom: 1.5rem;
      padding: 0.5rem;
      border: 2px solid #db6d6d;
      border-radius: 4px;
      background-color: #f9f9f9;
      cursor: pointer;
      width: 100%;
    }

    button {
      background-color: #98b9db;
      color: white;
      border: none;
      padding: 0.75rem 1.5rem;
      border-radius: 4px;
      font-size: 1rem;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    button:hover {
      background-color: #0056b3;
    }

    @media (max-width: 480px) {
      .container {
        padding: 1rem;
      }

      h1 {
        font-size: 1.5rem;
      }

      button {
        width: 100%;
        padding: 0.5rem;
      }
    }
  </style>
</head>
<body>
  <!-- Navigation Bar -->
  <nav>
    Hello Aman, You Can Upload a File
  </nav>
  
  <!-- Form Container -->
  <div class="container">
    <h1>File Upload</h1>
    <form action="/upload" method="post" enctype="multipart/form-data">
      <input type="file" name="profileimage" />
      <button type="submit">Upload</button>
    </form>
  </div>
</body>
</html>